apiVersion: v1
kind: Service
metadata:
  name: {{ include "dockerwalkthrough.fullname" . }}
  labels:
    {{- include "dockerwalkthrough.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dockerwalkthrough.selectorLabels" . | nindent 4 }}
